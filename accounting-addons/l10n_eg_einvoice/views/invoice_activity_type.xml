<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="invoice_activity_type_tree" model="ir.ui.view">
        <field name="name">invoice.activity.type.tree</field>
        <field name="model">invoice.activity.type</field>
        <field name="arch" type="xml">
            <list editable="bottom" decoration-muted="(not active)">
                <field name="code"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="invoice_activity_type_action" model="ir.actions.act_window">
        <field name="name">Invoice Activity Type</field>
        <field name="res_model">invoice.activity.type</field>
        <field name="view_mode">list</field>
        <field name="context">{'active_test': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new invoice activity type
            </p>
        </field>
    </record>
    
    <menuitem id="invoice_activity_type_menu" 
              name="Invoice Activity Type" 
              action="invoice_activity_type_action" 
              parent="account.account_management_menu"
              sequence="50"/>

</odoo>
