<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="update_crossovered_budget_form" model="ir.ui.view">
        <field name="name">crossovered.budget.form</field>
        <field name="model">crossovered.budget</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="amount_type"/>
                        <label for="amount" string="Amount"/>
                        <div class="o_row">
                            <field name="amount"/>
                            <span class="oe_form_field"
                                  invisible="amount_type != percentage"
                            > % </span>
                        </div>
                        <field name="company_id" invisible="1"/>
                        <field name="date_from" invisible="1"/>
                        <field name="date_to" invisible="1"/>
                        <field name="state" invisible="1"/>
                    </group>
                </group>
                <notebook>
                    <page string="Budget Lines">
                        <field name="crossovered_budget_line" context="{'default_date_from': date_from,'default_date_to': date_to}" colspan="4" nolabel="1">
                            <list decoration-success="is_above_budget and planned_amount &gt; 0" decoration-danger="is_above_budget and planned_amount &lt; 0" editable="bottom">
                                <field name="general_budget_id" domain="['|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"/>
                                <field name="analytic_account_id" domain="['|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]" groups="analytic.group_analytic_accounting"/>
                                <field name="date_from"/>
                                <field name="date_to"/>
                                <field name="paid_date" groups="base.group_no_one"/>
                                <field name="currency_id" invisible="1"/>
                                <field name="planned_amount" readonly="1"/>
                                <field name="new_amount" string="New Planned Amount" sum="New Planned Amount"/>
                                <field name="difference_amount" string="Difference Amount" sum="Difference Amount"/>
                                <field name="practical_amount" sum="Practical Amount"/>
                                <field name="theoritical_amount" sum="Theoretical Amount"/>
                                <field name="percentage" widget="percentage"/>
                                <button type="object" name="action_open_budget_entries" string="Entries..." icon="fa-arrow-circle-o-right"/>
                                <field name="is_above_budget" invisible="1"/>
                            </list>
                            <form string="Budget Lines">
                                <group>
                                    <group>
                                        <field name="currency_id" invisible="1"/>
                                        <field name="general_budget_id"/>
                                        <field name="new_amount" string="Planned Amount"/>
                                        <field name="analytic_account_id" domain="['|', ('company_id', '=', parent.company_id), ('company_id', '=', False)]" groups="analytic.group_analytic_accounting"/>
                                    </group>
                                    <group>
                                        <label for="date_from" string="Period"/>
                                        <div>
                                            <field name="date_from" class="oe_inline"/>
                                            -
                                            <field name="date_to" class="oe_inline"/>
                                        </div>
                                        <field name="paid_date" groups="base.group_no_one"/>
                                        <field name="company_id" options="{'no_create': True}" groups="base.group_multi_company"/>
                                    </group>
                                </group>
                            </form>
                        </field>
                    </page>
                </notebook>
                <footer>
                    <button name="action_update" string="Confirm" type="object" class="oe_highlight"/>
                    <button string="Cancel" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="inherit_crossovered_budget_form" model="ir.ui.view">
        <field name="name">inherit.crossovered.budget.form</field>
        <field name="model">crossovered.budget</field>
        <field name="inherit_id" ref="account_budget.crossovered_budget_view_form"/>
        <field name="arch" type="xml">
            <field name="planned_amount" position="after">
                <field name="new_amount" force_save="1" invisible="1"/>
            </field>
            <field name="state" position="before">
                <button name="action_update_budget" string="Update Budget" type="object" states="confirm,validate"/>
            </field>
        </field>
    </record>

</odoo>
