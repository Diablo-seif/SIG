<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="job_offer_action_reason_action" model="ir.actions.act_window">
        <field name="name">Rejection Reasons</field>
        <field name="res_model">action.reason</field>
        <field name="view_mode">list</field>
        <field name="context">{'default_res_model': 'hr.job.offer'}</field>
        <field name="groups_id" eval="[(4, ref('base.group_system')),(4, ref('hr.group_hr_manager'))]"/>
    </record>

    <menuitem id="action_reason_menu"
              name="Rejection Reasons"
              action="job_offer_action_reason_action"
              parent="hr_recruitment.menu_hr_recruitment_configuration"/>

</odoo>
